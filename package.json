{
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "start": "pnpm --filter docs-site run start",
    "start:todo-tutorial": "pnpm --filter todo-app-tutorial run dev",
    "build": "pnpm --filter docs-site run build"
  }
}
